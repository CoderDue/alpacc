# Wire Protocol of Generated Programs

Programs generated by the C backend (`alpacc c`) and the CUDA backend
(`alpacc cuda`), the Futhark test harness (`test` entry point of
`alpacc futhark` output), and the `alpacc test generate` / `alpacc test
compare` tooling all speak one binary protocol: grammar-native integer
types in **little-endian** byte order (alpacc targets little-endian
hosts, so this is host byte order).

The protocol has two framings:

- **Batch mode** (default, no flag): the stream starts with a `u64
  num_tests` count, followed by that many request frames. The output
  starts with the same count, followed by one response record per
  frame. This is the format of the `.inputs` / `.outputs` files
  produced by `alpacc test generate`.

- **Server mode** (`--server`): a loop of counted batches. The program
  repeatedly reads a `u64 num_tests` count followed by that many request
  frames, echoes the count, and flushes after each response record,
  until EOF. Meant for embedding the program in other tools; a client
  typically sends one count-1 batch per request. The count lets the
  program bound each exchange up front rather than trusting an
  open-ended stream.

Equivalently: the server stream is a concatenation of one or more batch
streams, with per-response flushing.

Both framings exist in all three build modes: lexer-only (`--lexer`),
parser-only (`--parser`), and combined (default).

## Native types

The code generator picks the smallest unsigned integer type that fits each
value domain of the grammar:

- `terminal_t` — token/terminal ids.
- `production_t` — production ids; only defined in builds that contain a
  parser.
- `index_t` — input positions (token starts); signed `int64_t` by default,
  or `int32_t` when the grammar uses `--index32`.
- `length_t` — token lengths (end − start). Unsigned. Defaults to the
  unsigned counterpart of `index_t` (`uint64_t` or `uint32_t`). Can be
  narrowed via `length = 8 | 16 | 32 | 64` in the grammar's `params {}`
  block to save memory when tokens are guaranteed short (e.g. `length = 8`
  for grammars where no token exceeds 255 bytes). Lexing fails if a token's
  length does not fit in `length_t`.

All backends derive these types from the same analysis, so a given
grammar gets identical type sizes everywhere.

Because a client cannot know these sizes in advance, every generated binary
supports:

```
$ ./prog --layout
terminal_t=1
production_t=1
index_t=8
length_t=8
```

`--layout` prints `key=value` lines (sizes in bytes) and exits. Lexer-only
and combined builds print `length_t`; parser-only builds omit it. Clients
should treat unknown keys as harmless and absent keys as "not used".

## Request frame (all modes)

```
u64  frame_len          length in bytes of the content that follows
u64  n                  ┐
payload                 ┘ content (frame_len = 8 + payload bytes)
```

The payload depends on the build mode:

- **lexer-only and combined**: `n` raw input bytes (the text to lex/parse).
- **parser-only**: `n` token ids, each `sizeof(terminal_t)` bytes
  (`frame_len` must equal `8 + n * sizeof(terminal_t)`).

## Response record

Every response starts with a validity byte:

```
u8   valid              1 = success, 0 = the input was rejected
```

If `valid == 0`, nothing else follows for this record. If `valid == 1`:

**Lexer-only** — the lexemes:

```
u64          num_lexemes
per lexeme:
  terminal_t token id
  index_t    start  (inclusive byte offset)
  length_t   length (token length in bytes; end = start + length)
```

**Parser-only** — the leftmost derivation:

```
u64          num_productions
production_t × num_productions
```

**Combined** — the tokens exactly as the lexer produced them, followed by
a compact productions-only parse tree. The tree is the preorder leftmost
derivation with the terminal slots dropped; parents are indices into the
compacted node arrays (the root's parent is 0, its own index). Each token
gets its parent node's index in `token_parents`; token `i` corresponds to
the `i`-th terminal slot of the underlying derivation. All sections are
structure-of-arrays:

```
u64          num_tokens
terminal_t   × num_tokens   token ids
index_t      × num_tokens   starts  (inclusive byte offsets)
length_t     × num_tokens   lengths (token lengths; end = start + length)
u64          num_nodes
production_t × num_nodes    production ids (preorder)
index_t      × num_nodes    parents (index of parent node)
index_t      × num_tokens   token_parents (index of each token's parent node)
```

## Backend differences

The backends implement identical wire formats. The differences are
operational:

- **CUDA tuning flags**: `--block-size`, `--items-per-thread`,
  `--shared-memory`, `--benchmark`, `--warmup` exist only in CUDA builds.
- **Futhark harness**: the generated `.fut` program exposes the batch
  protocol through its `test` entry point (bytes in, bytes out); there
  is no server loop. When run via `futhark script`, the output carries
  Futhark's own 15-byte binary-array header before the batch stream.

## Example client

`examples/server_client.py` demonstrates the intended embedding pattern:
query `--layout` once, keep a `--server` process running, and exchange one
count-1 batch per request:

```
$ alpacc c grammars/arithmetic.alp -o prog.c && cc -O2 -o prog prog.c
$ python3 examples/server_client.py ./prog both "1 + 2 * x"
```

It works unchanged with C and CUDA binaries in all three build modes
because the frame layout is fully described by `--layout`.

The integration suites (`tests/testc.sh`, `tests/testcuda.sh`) additionally
verify on every random grammar that feeding the batch input twice to a
server-mode process yields the batch output twice, byte-identically.
